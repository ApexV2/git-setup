name: lint

on:
    pull_request:
        types: [opened, synchronize]
        branches: [dev, main]

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2
            - run: pnpm i --frozen-lockfile eslint
            - run: pnpx eslint
